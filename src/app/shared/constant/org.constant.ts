export const constIconTypeOptions: Map<string, string> = new Map([
    ['nzIcon', 'nzIcon'],
]);

export const constIconThemeOptions: Map<string, string> = new Map([
    ['outline', 'outline']
]);

export const constMenuTypeOptions: Map<number, string> = new Map([
    [1, 'parent'],
    [2, 'child']
]);

export const alertTypeOptions = {
    alert: 1,
    notification: 2
};

export const engineTypeOptions: Map<number, string> = new Map([
    [0, 'Service'],
    [1, 'Switch']
]);

export const unitTypeOptions: Map<string, string> = new Map([
    ['psp', 'PSP']
]);


